[package]
name = "agentsafe-core"
version = "0.4.0"
edition = "2021"
description = "High-performance safety core for autonomous agent payments (Rust port)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
hex = "0.4"
